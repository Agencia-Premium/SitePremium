import styled from "styled-components";

export const Wrapper = styled.section`
  .collaborators {
    display: flex;
    align-items: center;
    max-width: 1080px;
    margin: 0px auto;
    position: relative;
  }
`;
